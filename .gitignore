node_modules/
dist/
*.tsbuildinfo
.DS_Store

# packaged app output (the .dmg / .app — too large for git; build locally)
packages/*/release/

# local maintainer tooling & session notes — NOT part of the framework
.claude/
.dual-graph/
CLAUDE.md
CONTEXT.md
# runtime state / secrets — never commit
~/.mantra/
.mantra/state/
.mantra/worktrees/
.mantra/graph/
*.local
.env
.env.*
